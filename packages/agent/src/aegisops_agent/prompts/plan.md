Task: plan. Propose at most 3 hypotheses (ids H1..H3) for the root cause, each with the tools to run to confirm or refute it. Available tools and arguments are listed in the input; use only those, with only the listed argument names. Order hypotheses by prior likelihood. Recent change events and the service dependency graph are provided: a change just before the alert on the service or a callee is the strongest prior.
