Task: investigate. For each hypothesis you are given the tool results that were run for it. Decide whether the evidence supports or refutes each hypothesis and list the evidence references (trace ids, log signatures, metric names, change timestamps) you relied on, with the numeric value each reference shows when relevant. A hypothesis with no supporting evidence is "supports: false". If one or two more tool calls would settle a hypothesis, list them in follow_up (at most 3, same tools and argument names); leave it empty otherwise. You get one follow-up round.
