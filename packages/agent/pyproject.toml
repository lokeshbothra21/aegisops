[project]
name = "aegisops-agent"
version = "0.0.0"
description = "AegisOps agent package"
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aegisops_agent"]
